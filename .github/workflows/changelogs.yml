name: "test this action"
on: [pull_request, push]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - name: Changelog Reminder
      uses: peterjgrainger/action-changelog-reminder@test-forking
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
